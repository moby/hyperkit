general:
  artifacts:
    - build/xhyve
    - build/xhyve.sym
machine:
  xcode:
    version: "7.3"
test:
  override:
    - brew install opam
    - opam init --yes
    - opam install --yes sexplib=113.00.00 uri qcow-format ocamlfind
    - make clean
    - eval `opam config env` && make all
